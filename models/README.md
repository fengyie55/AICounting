# Models Directory

Place your YOLOv8 model files here:

- `yolov8n.pt` - YOLOv8n detection model (will be automatically downloaded during build)
- `your_trained_model.pt` - Your custom trained model (if any)

The build script will automatically download yolov8n.pt if it's not present.
